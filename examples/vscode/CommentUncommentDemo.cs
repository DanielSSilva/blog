using System;

namespace CommentUncommentDemo
{
    class Program
    {
        static void Main(string[] args)
        {
            int a = 10;
            int b = 20;
            int sum = a + b;
            Console.WriteLine("The sum of a and b is: " + sum);

            // Uncomment and comment the following lines to demonstrate commenting/uncommenting in action:

            // int c = 30;
            // int product = a * c;
            // Console.WriteLine("The product of a and c is: " + product);

            string[] long_array = { "This", "is", "a", "long", "array", "of", "strings", "to", "demonstrate", "word", "wrap", "." };

        }
    }
}